# RigPulse v0.3.7

- Fixes an internal server error when running diagnostics on SHA-256 miners.
- Closes popup dialogs when the shaded area outside the dialog is clicked.
- Preserves the underlying miner detail view when its diagnostics dialog closes.
